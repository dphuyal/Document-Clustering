This project is an attempt to get fimilar with text clustering algorithms; K-Means and DBSCAN. Those algorithms are implemented and compared with the reference to National Science Foundation (NSF) dataset.
